import React from 'react';
import classes from './Slogan.module.css';

const slogan = () => {
    return (
        <React.Fragment>
            <h1 className={classes.primary}>MemoCats App</h1>
        </React.Fragment>
    );
};

export default slogan